name: Update Release Progress Badge

on:
  push:
    paths:
      - "progress/latest-release.md"
  workflow_dispatch:

jobs:
  update-progress:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Compute progress
        run: python .github/scripts/compute_release_progress.py

      - name: Commit updated badge
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .github/release-progress.json
          git commit -m "Update release progress badge" || echo "No changes"
          git push
